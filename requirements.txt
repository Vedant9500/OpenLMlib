numpy>=1.23,<3.0
mcp>=1.0.0,<2.0.0
sentence-transformers>=2.2,<5.0
rich>=12.0.0,<15.0.0
tomli>=2.0.0; python_version<'3.11'
tomli-w>=1.0.0,<2.0.0
# Optional: faiss-cpu>=1.7.4 (or faiss-gpu) for vector search
# Optional: hnswlib as a lightweight ANN alternative
# Dev: suite uses stdlib unittest (python -m unittest discover -s tests -p "test_*.py")
